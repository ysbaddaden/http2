language: generic
sudo: required

services:
  - docker

env:
  global:
    - H2SPEC=2.1.0

before_install:
  - docker pull alpine

script:
  - docker run -it --rm -e CI -e H2SPEC -w /work -v $PWD:/work alpine bin/ci docker
